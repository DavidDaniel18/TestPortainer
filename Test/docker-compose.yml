version: '3.4'

services:
  test:
    image: ${DOCKER_REGISTRY-}test
    build:
      context: .
      dockerfile: Test/Dockerfile
    ports:
      - "80"
      - "443"
      - "32790"
      - "32791"

